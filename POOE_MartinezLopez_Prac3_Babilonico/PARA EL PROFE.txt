Que tal profe, solo para decirle que el otro archivo llamada "BABYLON.ccp", es donde cree un algoritmo para sacar las raices cuadradas como viene en el diagrama de flujo, pero el algoritmo no pude implementarlo con el codigo que le entrego como tarea y por ello use la funcion "sqrt" directamente para sacar la raiz cuadrda, le paso ambos codigos y si en la clase me puede explicar como implementaria ambas cosas se lo agradeceria, saludos!
